---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="About" description="Future AU is a podcast about the bold ideas shaping Australia's next chapter. Learn about the show, the format, and how to get involved.">

  <!-- About Hero -->
  <section class="about-hero">
    <div class="about-hero-gold"></div>
    <div class="container">
      <div class="about-hero-content">
        <div class="about-label-row">
          <span class="about-line"></span>
          <span class="about-label">ABOUT THE SHOW</span>
        </div>
        <h1 class="about-headline">AUSTRALIA<br/>DESERVES<br/>BOLDER<br/>IDEAS</h1>
        <p class="about-intro">
          Future AU is a podcast about the people and ideas reimagining what Australia could become. Not what's wrong — what's possible.
        </p>
      </div>
    </div>
  </section>

  <!-- Mission -->
  <section class="about-mission">
    <div class="about-mission-bar"></div>
    <div class="container">
      <div class="mission-grid">
        <div class="mission-left">
          <h2 class="mission-title">THE THESIS</h2>
        </div>
        <div class="mission-right">
          <p class="mission-text">
            Australia punches below its weight. We've got the talent, the resources, and the geography — but too often, the conversation stays small. Safe. Incremental.
          </p>
          <p class="mission-text">
            Future AU exists to change that. Each episode is a deep conversation with someone who's thinking bigger: founders rethinking industries, researchers challenging assumptions, leaders pushing for structural change.
          </p>
          <p class="mission-text">
            The goal isn't to predict the future. It's to find the people already building it and give them a microphone.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- What to Expect -->
  <section class="about-format">
    <div class="container">
      <h2 class="format-title">WHAT TO EXPECT</h2>
      <div class="format-grid">
        <div class="format-card">
          <div class="format-number">01</div>
          <h3 class="format-card-title">Deep Conversations</h3>
          <p class="format-card-desc">
            Long-form interviews that give ideas room to breathe. No soundbites — just honest, substantive conversations about where Australia could go.
          </p>
        </div>
        <div class="format-card">
          <div class="format-number">02</div>
          <h3 class="format-card-title">Original Research</h3>
          <p class="format-card-desc">
            Every episode is backed by research. We dig into the data, the context, and the nuance before the mic turns on — so the conversation starts from a higher floor.
          </p>
        </div>
        <div class="format-card">
          <div class="format-number">03</div>
          <h3 class="format-card-title">Bold Guests</h3>
          <p class="format-card-desc">
            We're looking for people with unconventional takes — not talking heads repeating the consensus, but original thinkers pushing the conversation forward.
          </p>
        </div>
        <div class="format-card">
          <div class="format-number">04</div>
          <h3 class="format-card-title">Visual Storytelling</h3>
          <p class="format-card-desc">
            Each episode comes with data-driven slides shared across social — making the ideas shareable and the research accessible beyond the audio.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Topics We Cover -->
  <section class="about-topics">
    <div class="about-topics-bar"></div>
    <div class="container">
      <div class="topics-intro-grid">
        <div class="topics-intro-left">
          <h2 class="topics-intro-title">TOPICS<br/>WE COVER</h2>
        </div>
        <div class="topics-intro-right">
          <p class="topics-intro-text">
            Mobility and transport. Healthcare and aged care. Education and the future of work. Sport, culture, and national identity. Urban planning. Energy. Technology. Immigration.
          </p>
          <p class="topics-intro-text">
            If it shapes Australia's future, we want to talk about it — with the right person.
          </p>
          <a href="/#topics" class="topics-intro-link">SEE UPCOMING TOPICS →</a>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA for Guests -->
  <section class="about-cta">
    <div class="about-cta-gold"></div>
    <div class="about-cta-teal"></div>
    <div class="about-cta-content">
      <h2 class="about-cta-headline">
        <span class="about-cta-headline-gold">GOT A BOLD IDEA?</span>
        <span class="about-cta-headline-teal">WE WANT TO HEAR IT.</span>
      </h2>
      <p class="about-cta-desc">
        Know someone who should be on the show? Want to suggest a topic? We're always looking for the next great conversation.
      </p>
      <a href="/#contact" class="about-cta-button">GET IN TOUCH →</a>
    </div>
  </section>

</BaseLayout>

<style>
  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 24px;
  }

  /* ===== ABOUT HERO ===== */
  .about-hero {
    position: relative;
    overflow: hidden;
    background-color: var(--teal-dark, #0a3a3a);
    padding: 140px 0 96px;
  }

  .about-hero-gold {
    position: absolute;
    top: 0;
    right: 0;
    width: 40%;
    height: 100%;
    background-color: var(--gold, #f5c518);
    clip-path: polygon(30% 0, 100% 0, 100% 100%, 0% 100%);
  }

  .about-hero-content {
    position: relative;
    z-index: 10;
    max-width: 650px;
  }

  .about-label-row {
    display: flex;
    align-items: center;
    gap: 12px;
    margin-bottom: 32px;
  }

  .about-line {
    width: 48px;
    height: 4px;
    background-color: var(--gold, #f5c518);
  }

  .about-label {
    font-size: 14px;
    font-weight: 700;
    letter-spacing: 0.2em;
    color: var(--gold, #f5c518);
  }

  .about-headline {
    font-family: 'Bebas Neue', sans-serif;
    font-size: clamp(56px, 10vw, 110px);
    line-height: 0.9;
    color: #ffffff;
    margin-bottom: 32px;
  }

  .about-intro {
    font-size: 20px;
    color: #ffffff;
    line-height: 1.6;
    max-width: 500px;
  }

  /* ===== MISSION ===== */
  .about-mission {
    background-color: var(--cream, #f5f3e8);
    padding: 96px 0;
    position: relative;
  }

  .about-mission-bar {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 4px;
    background-color: var(--gold, #f5c518);
  }

  .mission-grid {
    display: grid;
    grid-template-columns: 1fr 1.5fr;
    gap: 64px;
    align-items: start;
  }

  .mission-title {
    font-family: 'Bebas Neue', sans-serif;
    font-size: clamp(48px, 8vw, 72px);
    line-height: 0.9;
    color: var(--teal-dark, #0a3a3a);
  }

  .mission-text {
    font-size: 18px;
    line-height: 1.8;
    color: var(--teal-dark, #0a3a3a);
    margin-bottom: 24px;
  }

  .mission-text:last-child {
    margin-bottom: 0;
  }

  /* ===== FORMAT ===== */
  .about-format {
    background-color: var(--teal-dark, #0a3a3a);
    padding: 96px 0;
  }

  .format-title {
    font-family: 'Bebas Neue', sans-serif;
    font-size: clamp(48px, 8vw, 72px);
    line-height: 0.9;
    color: #ffffff;
    margin-bottom: 64px;
  }

  .format-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 32px;
  }

  .format-card {
    border: 2px solid var(--teal-mid, #156b6b);
    padding: 40px;
    transition: border-color 0.3s ease;
  }

  .format-card:hover {
    border-color: var(--gold, #f5c518);
  }

  .format-number {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 48px;
    color: var(--gold, #f5c518);
    line-height: 1;
    margin-bottom: 16px;
  }

  .format-card-title {
    font-size: 20px;
    font-weight: 700;
    color: #ffffff;
    margin-bottom: 12px;
  }

  .format-card-desc {
    font-size: 16px;
    line-height: 1.7;
    color: var(--teal-light, #7ab5b5);
  }

  /* ===== TOPICS INTRO ===== */
  .about-topics {
    background-color: var(--cream, #f5f3e8);
    padding: 96px 0;
    position: relative;
  }

  .about-topics-bar {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 4px;
    background-color: var(--gold, #f5c518);
  }

  .topics-intro-grid {
    display: grid;
    grid-template-columns: 1fr 1.5fr;
    gap: 64px;
    align-items: start;
  }

  .topics-intro-title {
    font-family: 'Bebas Neue', sans-serif;
    font-size: clamp(48px, 8vw, 72px);
    line-height: 0.9;
    color: var(--teal-dark, #0a3a3a);
  }

  .topics-intro-text {
    font-size: 18px;
    line-height: 1.8;
    color: var(--teal-dark, #0a3a3a);
    margin-bottom: 24px;
  }

  .topics-intro-link {
    display: inline-block;
    font-size: 14px;
    font-weight: 700;
    letter-spacing: 0.1em;
    color: var(--teal-dark, #0a3a3a);
    background-color: var(--gold, #f5c518);
    padding: 14px 28px;
    transition: background-color 0.3s ease;
  }

  .topics-intro-link:hover {
    background-color: var(--gold-hover, #f7d04c);
  }

  /* ===== ABOUT CTA ===== */
  .about-cta {
    position: relative;
    padding: 96px 0;
    overflow: hidden;
  }

  .about-cta-gold {
    position: absolute;
    inset: 0;
    background-color: var(--gold, #f5c518);
  }

  .about-cta-teal {
    position: absolute;
    top: 0;
    right: 0;
    width: 45%;
    height: 100%;
    background-color: var(--teal-dark, #0a3a3a);
    clip-path: polygon(20% 0, 100% 0, 100% 100%, 0% 100%);
  }

  .about-cta-content {
    position: relative;
    z-index: 10;
    max-width: 700px;
    margin: 0 auto;
    padding: 0 24px;
    text-align: center;
  }

  .about-cta-headline {
    font-family: 'Bebas Neue', sans-serif;
    font-size: clamp(36px, 6vw, 64px);
    line-height: 1;
    margin-bottom: 24px;
  }

  .about-cta-headline-gold {
    display: inline-block;
    background-color: var(--gold, #f5c518);
    color: var(--teal-dark, #0a3a3a);
    padding: 8px 16px;
  }

  .about-cta-headline-teal {
    display: inline-block;
    background-color: var(--teal-dark, #0a3a3a);
    color: #ffffff;
    padding: 8px 16px;
    margin-top: 8px;
  }

  .about-cta-desc {
    font-size: 17px;
    color: var(--teal-dark, #0a3a3a);
    max-width: 480px;
    margin: 0 auto 40px;
    line-height: 1.6;
    background-color: rgba(245, 197, 24, 0.9);
    padding: 16px;
    display: inline-block;
  }

  .about-cta-button {
    display: inline-block;
    padding: 16px 40px;
    background-color: var(--teal-dark, #0a3a3a);
    color: #ffffff;
    font-weight: 700;
    font-size: 16px;
    letter-spacing: 0.1em;
    transition: background-color 0.3s ease;
  }

  .about-cta-button:hover {
    background-color: var(--teal-mid, #156b6b);
  }

  /* ===== RESPONSIVE ===== */
  @media (max-width: 1024px) {
    .format-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 768px) {
    .about-hero-gold {
      width: 100%;
      height: 30%;
      top: auto;
      bottom: 0;
      clip-path: polygon(0 40%, 100% 0, 100% 100%, 0 100%);
    }

    .mission-grid,
    .topics-intro-grid {
      grid-template-columns: 1fr;
      gap: 32px;
    }

    .format-grid {
      grid-template-columns: 1fr;
    }

    .about-cta-teal {
      display: none;
    }
  }
</style>
